{# @var type \Jmondi\Gut\Infrastructure\Describer\ApplicationReflectionClass #}
export class {{ type.shortName }} {

    public {{ type.shortName }}() {
        return [
        {% for typeNewKey in typeDetails %}
        {
            {% for key, value in typeNewKey.jsonSerialize %}
                "{{ key }}": "{{ value }}",
            {% endfor %}
        },
        {% endfor %}
        ]
    }
}
