{# @var type \Jmondi\Gut\Infrastructure\Describer\ApplicationReflectionClass #}

import { RestClientInterface } from '../../RestClient/rest-client';

export class {{ type.typeDomain }} {
    public {{ type.shortName | lowercaseFirst }}(userId: string, withData: string[] = null) {
        return this.restClient.get(
            'api/v1/Action/User/GetUser',
            {
            {# @var value \ReflectionMethod #}
            {%- for param in type.constructor.parameters -%}
                {{ param.name }},
            {% endfor -%}
            }
        );
    }
}
